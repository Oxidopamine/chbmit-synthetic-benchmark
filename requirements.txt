# Core scientific stack
numpy>=1.24,<2.0
scipy>=1.10
pandas>=2.0
pyyaml>=6.0
tqdm>=4.65

# EEG / EDF IO
pyedflib>=0.1.36        # the only EDF reader used (chbmit/preprocess_edf.py)

# ML
# NOTE: torch is deliberately unpinned here, but the GPU grids in results_chbmit_synthetic/ were
# produced on Vertex's pytorch-gpu.2-4.py310 image. Record torch.__version__ alongside any new
# grid -- the committed CSVs predate that practice and their exact torch build is unrecoverable.
torch>=2.1          # CPU build on Windows by default (no CUDA GPU present)
scikit-learn>=1.3

# Storage
zarr>=2.16,<3.0
numcodecs>=0.12

# Visualization / quality checks
matplotlib>=3.7

# SzCORE-aligned event scoring (ESL-EPFL timescoring)
timescoring>=0.0.5

# Testing
pytest>=7.4
